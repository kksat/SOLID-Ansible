---
- name: Test sample role
  hosts: localhost
  gather_facts: false
  vars: {}
  tasks:
    - name: Show copyright
      solid.example.show_c:

    - name: Show warranty
      solid.example.show_w:

    - name: Run sample role
      ansible.builtin.include_role:
        name: solid.example.simple_role
